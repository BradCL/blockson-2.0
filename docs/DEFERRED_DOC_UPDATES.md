# Deferred documentation updates

Some docs are **frozen**: they are snapshot/teaching artifacts that read as
coherent wholes and are expensive to keep in lock-step with the code (the
learning guide is large; the tutorials carry captured screenshots and terminal
transcripts that must be re-generated by Playwright harnesses). Rather than
update them on every change — which costs effort out of proportion to the value,
since a learner doesn't need them to track the latest tweak — we let them lag and
record what they owe **here**, then reconcile in a single bundled pass.

**Live docs are not listed here.** `SPEC.md`, `OPERATOR.md`, and
`BLOCK_CATALOG.md` are normative/reference and are updated with the change that
affects them. This ledger is only for the frozen artifacts below.

**How to use it.** When a change lands that a frozen doc should eventually
reflect, add a one-line entry under that doc (what changed + which
chapter/section/capture it touches). When you do a reconciliation pass: load the
frozen doc once, apply every entry, update its baseline, and clear the entries.
The baseline commit also gives a git fallback — `git log <baseline>..HEAD -- engine/`
is the raw delta if an entry was ever missed.

---

## Learning guide (`docs/learning-guide/`)

**Baseline:** last reconciled at `13242e1` (2026-06-27, per-page page-header
background). Owner-facing engine features have landed since without being
reflected.

Pending:

- **Per-section doorway** (`79191fd`) — the per-section hover chip, the
  `describeSection` / `GET /api/section` path, and the generalized creatable
  page-header subtitle. Affects the editor/overlay chapter
  (`02-atlas/07-dom-and-events.md`) and the owner-edit trace
  (`03-traces/trace-b-owner-edit.md`).
- **Editable CTA buttons** (`9d248ac`) — hero `actions` as addressable items
  (optional id + `add-action-ids.js` migration), the combined button editor, the
  scaffold empty-array relaxation. Touches the JSON-model / patch chapters and the
  trace's "what's editable" framing.
- **Image thumbnails in the editor** (`dc3b9cd`) — `renderImageEditor` now shows
  the current image and previews the chosen file. Small note in the DOM/editor
  chapter.
- **Keep the editor open after each save** (`f8be338`) — `stage()` no longer
  closes the editor; `renderPending()` renders the Now→After review inline via the
  `reopen` thunk, and Keep/Discard re-open the same editor. Update
  `02-atlas/07-dom-and-events.md` and `03-traces/trace-b-owner-edit.md` (the
  "shows the pending card" step is now an inline review that keeps the editor
  open).

## Developer tutorial (`docs/tutorial/developer/`)

**Baseline:** last reconciled at `e49cbc1` (2026-06-17, page-header default
background).

Pending:

- **Proof count drifted** — the suite is now **27** proofs (the tutorial's
  terminal captures and any "N proofs" prose predate the CTA-button proof 27 and
  the section-doorway proof 26). Re-capture `term/01-proofs.txt` if it pins a
  count.
- **Owner-facing features added since** (section doorway, CTA buttons, image
  thumbnails, keep-open) may warrant a mention where the tutorial describes what
  the handed-off editor can do, though the developer *workflow* itself is
  unchanged.

## Owner tutorial (`docs/tutorial/owner/`)

**Baseline:** last reconciled at `3000bc1` (2026-06-11). This is the most
expensive to reconcile — it has 32 captured PNG screenshots produced by
`scripts/flows/owner-editor.js` via `scripts/capture-tutorial.js`.

Pending:

- **Editor keep-open flow changes the screenshots** (`f8be338`) — the captures
  show a Save closing the editor and a standalone pending card; the editor now
  stays open with the review inline and Keep/Discard return to it. The
  `pending-card` / `keep` / `discard` captures and the surrounding prose need a
  re-capture, and `scripts/flows/owner-editor.js` steps may need updating to
  assert the inline review.
- **Owner-facing features not yet shown** — hero focal-point/zoom controls
  (`ba3e502`, `68d80c7`), reachable section backgrounds (`097e9fe`), the
  per-section doorway (`79191fd`), editable CTA buttons (`9d248ac`), image
  thumbnails (`dc3b9cd`), and editable gallery/photo-strip link labels
  (`01f77e8`). Decide which deserve a step; the tutorial need not cover every
  feature, but the pending-card flow re-capture above is required for accuracy.
